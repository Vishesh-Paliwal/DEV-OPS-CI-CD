apiVersion: v1
kind: Service
metadata:
  name: devops-api-service
  namespace: devops-demo
  labels:
    app: devops-api
spec:
  selector:
    app: devops-api
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP